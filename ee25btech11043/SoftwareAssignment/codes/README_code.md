  In the code For different images we can change the input image name in the code ,
  
  then we have to give the input k value.

  then also we can change the image generated image if we want.
  the code also prints the dimension of image and the frobenius error.
